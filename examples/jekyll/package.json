{
  "name": "jekyll-blog-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "blog:sync": "node scripts/blog/sync.mjs sync --json",
    "blog:build": "node scripts/blog/sync.mjs build --json",
    "blog:publish": "node scripts/blog/sync.mjs publish --json",
    "server": "bundle exec jekyll serve -P"
  },
  "devDependencies": {}
}
