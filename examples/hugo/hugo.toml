baseURL = 'https://example.com/'
languageCode = 'zh-cn'
title = 'My Blog'

# 同步脚本写入 content/posts/，slug 来自 frontmatter
[permalinks]
  posts = '/:slug/'

# Obsidian 笔记里常有内嵌 HTML，不开 unsafe 会被 Goldmark 吞掉
[markup.goldmark.renderer]
  unsafe = true
